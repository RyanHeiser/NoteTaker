<UserControl x:Class="NoteTaker.CustomControls.ListViewSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NoteTaker.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="{Binding Title}" />
        <TextBox Grid.Row="1" Name="selectionTextBox"  KeyDown="SelectionTextBox_KeyDown" LostFocus="SelectionTextBox_LostFocus" />
        <ListView Grid.Row="2" Name="selectionList" PreviewMouseLeftButtonUp="SelectionList_Click"></ListView>
    </Grid>
</UserControl>
